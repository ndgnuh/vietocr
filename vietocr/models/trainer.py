import random

import torch

from .mo

class Trainer:
    def __init__(self)
        self.model = 
        
model = models.OCRModel(len(vocab), {"name": "mlp_mixer_tiny", "image_height": 32}, "linear")
error: cannot format -: Cannot parse: 5:8: from .mo
